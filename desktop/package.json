{
  "name": "apphub-desktop",
  "version": "1.0.0",
  "private": true,
  "description": "App Hub 桌面壳层（Tauri v2 原生窗口 + 零编译启动器）",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "backend": "node scripts/start-backend.js",
    "frontend": "node scripts/build-frontend.js",
    "icons": "python scripts/make-icons.py",
    "verify": "python scripts/verify.py",
    "launch": "python launch.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
